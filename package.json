{
  "name": "safebridge-mcp",
  "version": "0.1.0",
  "description": "Privacy-aware MCP server that lets Claude Code query your codebase via DeepSeek with secret redaction, allowlist gating, budget caps, and a tamper-evident audit log.",
  "keywords": ["mcp", "claude", "deepseek", "privacy", "redaction", "llm", "codebase"],
  "repository": { "type": "git", "url": "https://github.com/your-org/safebridge-mcp.git" },
  "homepage": "https://github.com/your-org/safebridge-mcp#readme",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "node --test src/redact.test.js src/allowlist.test.js src/audit.test.js src/budget.test.js src/pseudonym.test.js",
    "test:watch": "node --test --watch src/redact.test.js src/allowlist.test.js src/audit.test.js src/budget.test.js src/pseudonym.test.js",
    "smoke": "node scripts/smoke.mjs",
    "check": "npm test && npm run smoke",
    "start": "node src/index.js",
    "verify-log": "node -e \"import('./src/audit.js').then(m=>{const r=m.verifyLog(process.argv[1]||process.env.SAFEBRIDGE_AUDIT_LOG||'../../.safebridge-audit.log');console.log(r);process.exit(r.ok?0:1)})\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "license": "MIT"
}
